package io.bettercommerce.screenmirror.ui.navigation

/**
 * App navigation destinations. Kept as simple route constants for the M0
 * skeleton; can migrate to type-safe Navigation routes later.
 */
object Routes {
    const val HOME = "home"
    const val SENDER = "sender"
    const val RECEIVER = "receiver"
    const val SETTINGS = "settings"
}
